#### Notes for Project ###


 :- free                               		== Using this command we can check the the system free memory space

 :- free -h                            		== Simple way for humans

 :- free -mt  			       		== in formate total use and total free etc

 :- free -mt | grep "Total" 	       		== This is sparate for us what we need only

 :- free -mt | grep "Total" | awk '{print $4}'  == also for sapraton

 :- df  					== For check the disk status

 :- df - H | egrep -v "Filesystem|tmpfs"

 :- df -H | egrep -v "Filesystem|tmpfs"


######## prpject no. 3rd  #############

:- In the given directory, if you fin fikes more than a given size ex: 20MB or files older than given days ex:10days, Sompress those files and move in a 'archive' folder.

:::- purpose of this script??
ans :- for space of disk , we are reduce size 


------------Steps of Scripts:-------------------

1. Provide the path of directory 
2. Check if the dire. is presrnt or note 
3. Create "Archive" folder if not already present
4. Find all the files with size more than 20MB
5. Compress each file 
6. Move the compressed file in "Archive" folder
7. Make a cron job to run the script every dat at given time

-------------------------------------------------


######## Project no. 4th ##########

In this project we are working on the local user create 

:- Requirement 

- Script should be executed with root user else exit with status 1 and error.
- Script will take 1st argument as user and rest willl be trated as comment.
- Auto generete password for the user
- Upon successful execution of script, display the following 
	- username : <username>
	- password : <Auto_genrate password>
	- host : 

-------------Steps of Script--------------------

1. Check id the script is being executed with superuser privileges.
2. If the user doesn't supply at least one argument, then give them help.
3. The first parameter is the user name.
4. The rest of the parameterare for the commnets.
5. Generate a password.
6. Create the user with the password.
7. Check to see if the useradd command succeeded.
8. Force password change on first login.
9. Display the username, password and the host where the user is created.


--------------------------------------------------


